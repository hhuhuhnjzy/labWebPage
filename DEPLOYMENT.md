# AgentFabric 网站部署指南

## 🚀 部署方案

### 方案 A: 使用 Vercel 部署（推荐）

**优点：** 免费、快速、自动部署、支持自定义域名

**步骤：**

1. 访问 [Vercel](https://vercel.com)
2. 使用 GitHub/GitLab 账号登录
3. 点击 "New Project"
4. 导入您的 Git 仓库
5. Vercel 会自动检测 Vite 项目
6. 点击 "Deploy"

**命令行部署：**
```bash
npm install -g vercel
vercel login
vercel
```

---

### 方案 B: 使用 Netlify 部署

**优点：** 免费、简单、支持表单处理

**步骤：**

1. 访问 [Netlify](https://netlify.com)
2. 使用 GitHub 账号登录
3. 点击 "Add new site" → "Import an existing project"
4. 选择您的仓库
5. 构建命令：`npm run build`
6. 发布目录：`dist`
7. 点击 "Deploy"

**命令行部署：**
```bash
npm install -g netlify-cli
netlify login
netlify deploy --prod
```

---

### 方案 C: GitHub Pages 部署

1. 修改 `vite.config.js`：
```js
export default defineConfig({
  base: '/repository-name/',  // 替换为您的仓库名
  plugins: [react()],
})
```

2. 添加部署脚本到 `package.json`：
```json
{
  "scripts": {
    "deploy": "vite build && gh-pages -d dist"
  }
}
```

3. 安装并部署：
```bash
npm install --save-dev gh-pages
npm run deploy
```

---

## 📝 内容管理方案

### 方案一：简单 JSON 文件管理

**特点：** 无需数据库，适合小型网站

**使用方法：**
1. 编辑 `public/data/people.json` 等文件
2. 推送到 Git 仓库
3. 自动重新部署

### 方案二：Headless CMS（推荐）

**推荐工具：**
- **Sanity.io** - 免费、实时预览、友好的UI
- **Strapi** - 开源、可自托管
- **Contentful** - 功能强大

### 方案三：自建管理后台

详见下方的管理后台实现方案。

---

## 🔧 自定义域名

### Vercel
1. 在项目设置中点击 "Domains"
2. 添加您的域名
3. 按照提示配置 DNS

### Netlify
1. 在站点设置中点击 "Domain management"
2. 添加自定义域名
3. 配置 DNS

---

## 🌍 环境变量

如果需要配置环境变量（如 API 密钥）：

**Vercel:**
项目设置 → Environment Variables

**Netlify:**
站点设置 → Environment variables

---

## 📊 监控和分析

推荐集成：
- **Google Analytics** - 网站分析
- **Vercel Analytics** - 性能监控
- **Sentry** - 错误追踪

---

## 🔄 自动部署

### 设置自动部署
1. 连接 GitHub 仓库
2. 每次推送到 main 分支自动部署
3. PR 预览：每个 PR 都有预览链接

### 部署触发条件
- 推送到主分支 → 生产部署
- 推送到其他分支 → 预览部署
- 手动触发 → 在平台控制台操作

---

## ⚡ 性能优化

部署前优化：
```bash
npm run build
```

检查构建大小：
```bash
npm run preview
```

---

## 🆘 常见问题

**Q: 部署后页面刷新 404？**
A: 确保配置了 SPA 路由重定向（vercel.json 或 netlify.toml）

**Q: 如何回滚到之前的版本？**
A: Vercel/Netlify 都支持在控制台回滚到之前的部署

**Q: 如何查看部署日志？**
A: 在平台控制台的 "Deployments" 页面查看

---

## 📞 获取帮助

- [Vercel 文档](https://vercel.com/docs)
- [Netlify 文档](https://docs.netlify.com)
- [Vite 部署指南](https://vitejs.dev/guide/static-deploy.html)

